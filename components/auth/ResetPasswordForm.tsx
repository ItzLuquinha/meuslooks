"use client";
import { useState } from 'react';
import { createClient } from '@/lib/supabase/client';
export default function ResetPasswordForm(){const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [msg,setMsg]=useState('');const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();if(password.length<8||password!==confirm){setMsg(password!==confirm?'As senhas não conferem.':'A senha precisa ter pelo menos 8 caracteres.');return;}setBusy(true);const s=createClient();const {error}=await s.auth.updateUser({password});if(error)setMsg('Não foi possível redefinir sua senha.');else{const sync=await fetch('/api/password/sync',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password})});if(!sync.ok)setMsg('Senha alterada, mas não foi possível concluir a sincronização de segurança.');else{setMsg('Senha alterada.');setTimeout(()=>window.location.href='/inicio',600)}}setBusy(false)}return <main className="auth-page"><section className="auth-card"><div className="auth-brand">Meu Look ♡</div><p className="auth-copy">Crie uma nova senha para continuar.</p><form className="auth-form" onSubmit={submit}><div className="field"><label className="label">Nova senha</label><input className="input" type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required/></div><div className="field"><label className="label">Confirmar nova senha</label><input className="input" type="password" minLength={8} value={confirm} onChange={e=>setConfirm(e.target.value)} required/></div>{msg&&<p className="card-copy">{msg}</p>}<button className="btn btn-primary" disabled={busy}>{busy?'Salvando…':'Salvar nova senha'}</button></form></section></main>}
