"use client";
import { useEffect,useState } from 'react';
export default function AdminMessagesClient(){const [form,setForm]=useState({title:'Uma mensagem para você ♡',body:'',active:true});const [msg,setMsg]=useState('');useEffect(()=>{fetch('/api/admin/messages').then(r=>r.json()).then(j=>j.message&&setForm({title:j.message.title,body:j.message.body,active:j.message.active}))},[]);async function save(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/admin/messages',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});setMsg(r.ok?'Mensagem atualizada.':'Não foi possível salvar.');}async function del(){const r=await fetch('/api/admin/messages',{method:'DELETE'});if(r.ok){setForm({...form,active:false});setMsg('Mensagem removida.')}}return <><h1 className="page-title">Mensagens</h1><p className="page-subtitle">Uma mensagem ativa aparece no topo da página inicial.</p>{msg&&<div className="card" style={{padding:13,marginTop:16}}>{msg}</div>}<section className="section"><form className="card form-card" onSubmit={save}><div className="field"><label className="label">Título</label><input className="input" value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/></div><div className="field" style={{marginTop:13}}><label className="label">Mensagem</label><textarea className="textarea" value={form.body} onChange={e=>setForm({...form,body:e.target.value})}/></div><label style={{display:'flex',alignItems:'center',gap:8,marginTop:13,fontWeight:700}}><input type="checkbox" checked={form.active} onChange={e=>setForm({...form,active:e.target.checked})}/>Ativa</label><div className="inline-actions" style={{marginTop:16}}><button className="btn btn-primary">Salvar mensagem</button><button type="button" className="btn btn-danger" onClick={del}>Excluir</button></div></form></section></>}
