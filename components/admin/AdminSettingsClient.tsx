"use client";
import { useState } from 'react';
export default function AdminSettingsClient(){const [msg,setMsg]=useState('');return <><h1 className="page-title">Configurações</h1><p className="page-subtitle">Opções administrativas do aplicativo.</p><section className="section"><div className="card form-card"><h2 className="section-title">Conta administrativa</h2><p className="page-subtitle" style={{marginTop:7}}>O acesso usa a conta administrativa configurada no servidor. A senha e a chave de criptografia não ficam no frontend.</p><button className="btn btn-danger" style={{marginTop:16}} onClick={async()=>{await fetch('/api/admin/logout',{method:'POST'});window.location.href='/login'}}>Encerrar sessão administrativa</button>{msg&&<div style={{marginTop:12}}>{msg}</div>}</div></section></>}
